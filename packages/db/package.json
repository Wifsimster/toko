{
  "name": "@focusflow/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "echo '⚠️  db:push is dev-only. Use db:generate + db:migrate for production.' && drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@focusflow/validators": "workspace:*",
    "drizzle-orm": "0.45.1",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "typescript": "^5.7.0"
  }
}
