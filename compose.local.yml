services:
  postgres:
    image: postgres:16-alpine
    container_name: toko-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${DB_USER:-toko}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-toko_secret}
      POSTGRES_DB: ${DB_NAME:-toko}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-toko}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

networks:
  internal:
    driver: bridge
